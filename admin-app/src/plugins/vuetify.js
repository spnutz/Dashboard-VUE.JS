import Vue from 'vue';
import Vuetify from 'vuetify/lib';

Vue.use(Vuetify, {
    iconfont: 'md',
    theme: {
        primary: '#9652ff',
        success: '#3cd1c2'

    }
});

export default new Vuetify({
});
